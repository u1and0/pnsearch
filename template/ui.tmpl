{{template "header"}}
<body>
  <form name="window" method="get" action="/search/ui">
    <!-- 検索キーワード入力 -->
    <label for="製番">製番</label>
    <input type="text" name="製番" value="{{ .query.ProductNo }}" size="10">
    <label for="要求番号">要求番号</label>
    <input type="text" name="要求番号" value="{{ .query.UnitNo }}" size="10">
    <label for="品番">品番</label>
    <input type="text" name="品番" value="{{ .query.Pid }}" size="10">
    <label for="品名">品名</label>
    <input type="text" name="品名" value="{{ .query.Name }}" size="10">
    <label for="形式寸法">形式寸法</label>
    <input type="text" name="形式寸法" value="{{ .query.Type }}" size="10">
    <label for="メーカ">メーカ</label>
    <input type="text" name="メーカ" value="{{ .query.Maker }}" size="10">
    <label for="仕入先">仕入先</label>
    <input type="text" name="仕入先" value="{{ .query.Vendor }}" size="10">

    <label for="orderby-発注">発注</label>
    <select id="発注" name="納入">
      <option value="全て">全て</option>
    </select>
    <label for="納入">納入</label>
    <select id="納入" name="納入">
      <option value="全て">全て</option>
      <option value="納入日 無し(未納入)">納入日 無し(未納入)</option>
    </select>
    <label for="orderby">並替</label>
    <select id="orderby" name="orderby">
      <option value="製番">製番</option>
      <option value="登録日">登録日</option>
      <option value="発注日">発注日</option>
      <option value="納期">納期</option>
      <option value="回答納期">回答納期</option>
      <option value="納入日">納入日</option>
    </select>
    <select id="asc" name="asc">
      <option value="true">昇順</option>
      <option value="false">降順</option>
    </select>
    <label for="method">複数語句の組み合わせ</label>
    <select id="method" name="method">
      <option value="AND">AND検索</option>
      <option value="OR">OR検索</option>
    </select>

    <!-- 表示設定 -->
    {{$querySelect := .query.Select}}
    {{ range $label := .labels }}
      <input type="checkbox" name="select" id="select-{{$label.Value}}" value="{{$label.Value}}"
      {{range $querySelect}}
        {{if eq $label.Value .}}checked{{end}}
      {{end}}
      >
      <label for="select-{{$label.Value}}">{{$label.Name}}</label>
    {{end}}

    <!-- 検索ボタン -->
    <input type="submit" id="submit" value="&#xf002;" class="fas">
  </form>
  {{ .msg }}
  {{template "table" .}}
